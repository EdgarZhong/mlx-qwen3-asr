# 2026-09 事件记录

## MEM-2026-09-18-001：CapsWriter 自动语言正文前缀

- Decision：CapsWriterRunnerConfig.auto_language_text_only 默认 True，通过 Session 同步/异步入口传递，未指定语言且无需时间戳/说话人对齐时预置 `<asr_text>`。裸 API 默认、显式语言和对齐入口保持原行为；输出语言如实为 unknown。正文模式按字符边界拼接 chunk，保留英文词间空格。
- Reuse next time：不要修改共享 tokenizer 或在服务端复制提示词策略；设配置 False 可恢复旧入口。诊断脚本旧 auto 必须显式关闭新默认。吞尾补静音证据不足，不能随语言修复一起启用。
- Evidence：父仓库 `tools/asr_symptom_probe.py --infer --production-regression`，`diagnogs/symptom_probe/20260918-151509/`；32条旧基线复现，32条新输出与前轮诊断一致，3条全英文异常恢复中英混排；175项定向测试通过。日常服务尚未重启，未声明全量准确率提升。
- 补充 Evidence：31.2秒实际录音/2个内部chunk，正文前后完全一致，记录在父仓库 `diagnogs/symptom_probe/20260918-151509-long/`；语法及主/子仓库差异检查通过。

## MEM-2026-09-18-002：修复声学编码器尾块补齐语义

- Decision：与Qwen官方pad_sequence一致，存在完整块时尾块先补零到chunk_size，再三层卷积，裁回原有效token数。仅单个短块不额外补齐；不向PCM追加静音、不改变8秒注意力策略。
- Reuse next time：不可把“直接卷积短尾块”当作“补齐后卷积并裁剪”的等价优化，非零bias/GELU可改变最后有效token。测试必须使用非零bias，并对照官方语义，不能只比较自己的旧循环。
- Evidence：父仓库 `diagnogs/tail_boundary/20260918-155332/` 和 `20260918-155453/`，两种prompt下同样4条恢复Windows尾字；旧编码器3项回归失败，修复后267项通过。另有Windows全局注意力与MLX8秒分窗差异，9条尾缀受影响，此策略未修改默认。
- 补充 Evidence：正式适配器33条/66次验收通过，64组短录音raw token/prompt/正文与诊断尾块修正一致；31.2秒长录音新默认完全不变。记录 `diagnogs/tail_boundary/production-fixed/acceptance.json`，语法/diff检查通过；服务尚未重启。
