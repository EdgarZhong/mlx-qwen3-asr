# 2026-07 Events

## MEM-2026-07-06-001: CapsWriter 专用 Runner 成为调优前置边界

Decision: 为 CapsWriter 接入新增 package-owned `QwenASRRunner`，让同一个 `task_id` 下的音频增量先进入 runner 生命周期，再由 runner 统一调用现有 `Session.transcribe()` 和集中配置；CapsWriter server 外层只负责传输、队列、模型路径和请求元信息。

Reuse next time: 后续评测 driver 与产品 server 都应调用同一 runner，而不是裸调 `Session.transcribe()`；影响 ASR 输出的参数优先落在 package 内的 runner 配置，CapsWriter 外层避免重复维护 generation、prompt、chunking、aligner 等策略。

Evidence: CapsWriter 根仓库 `docs/ASR调优总文档.md` 已明确 P0 先跑通本地源码包后端、按 `task_id` feed `AudioFeedPatch`、集中推理级配置，再开始正式调优矩阵。
